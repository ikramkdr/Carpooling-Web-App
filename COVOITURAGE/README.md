This repository contains a full-stack carpooling web application.

-  **Frontend** is located in the `landing/` folder (React + Vite)
-  **Backend** is located in the `backend/` folder (Node.js + Express)
